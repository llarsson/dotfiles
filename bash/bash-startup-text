# Display a fortune cookie if we are running interactively
if [ -n "$PS1" ]; then
    if which fortune; then
        fortune
    fi
    echo 
    echo ================================
    echo 
    if which pal; then 
        pal -r 7 -x 14
    fi
fi
